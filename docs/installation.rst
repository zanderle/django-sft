============
Installation
============

At the command line::

    $ easy_install django-sft

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-sft
    $ pip install django-sft
